const allArguments = process.argv;
const log = console.log;

// log(allArguments[0]);
// log(allArguments[1]);
// log(allArguments[2]);
log(allArguments.length);

log(process.env.PORT);
log(process.env.ABC);
